This repository contains the source for a new type of search engine that is specialized for healthcare.

### Patent pending ###

This search engine utilizes companies that expose their content via ReST APIs. After registering and verifying their domain, they can upload a Swagger doc that defines their APIs. Standard ReST or FHIR supported.
